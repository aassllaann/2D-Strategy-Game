# 易经大战略：执理者 (I Ching Grand Strategy: The Executor)

> 2D 策略游戏 · 完整设计提案 · 优化版

| 属性         | 描述                        |
| :----------- | :-------------------------- |
| **引擎**     | **Godot 4**                 |
| **视角**     | **混合 2D 策略 / 视觉小说** |
| **AI 后端**  | **Claude API**              |
| **团队规模** | **2人 (程序员 + 美术)**     |
| **开发周期** | **MVP 约 4~5 个月**         |

## 一、 项目概述

本提案是原“易经大战略：执理者”概念的优化修订版，调整了引擎选择、交互方式和视觉实现。在完整保留核心策略游戏体验的同时，将开发难度从 ★★★★☆ 降低至 ★★★☆☆，使 2 人团队能够在 4~5 个月内完成可玩的 MVP。

*   **保留核心：** 玩家扮演国师，通过解读卦象干预战局；64 卦状态机驱动 NPC 行为；多结局数值目标系统。
*   **修改核心：** 引擎从 Unity/Unreal 改为 Godot 4；3D 画面改为 2D 水墨像素风；自由文本输入改为结构化选项 + AI 叙事。

### 1.1 一句话简介

《执理者》是一款 2D 水墨风格的策略 + 视觉小说游戏：玩家作为随军谋士，通过解读易经卦象并选择战略方向，引导 AI 生成的战争叙事，最终影响国家命运。

### 1.2 目标受众

*   对中国传统文化和历史策略感兴趣的玩家 (18~40 岁)。
*   喜欢具有叙事深度且操作轻量化的视觉小说或策略游戏玩家。
*   Steam / itch.io 独立游戏受众。

## 二、 引擎与技术栈

### 2.1 完整技术栈

| 模块         | 方案                                                   |
| :----------- | :----------------------------------------------------- |
| **游戏引擎** | Godot 4.x (主要: GDScript)                             |
| **AI 后端**  | Claude API (claude-sonnet-3.5/4.6, 结构化 JSON 输出)   |
| **美术工具** | Aseprite (像素美术) / Procreate + Photoshop (水墨资产) |
| **音频工具** | FMOD 或 Godot 内置 AudioStreamPlayer + OGG 资产        |
| **版本控制** | Git + GitHub                                           |
| **配置管理** | 易经数据库 JSON 文件 (~64 条记录)                      |
| **存档系统** | Godot 内置 FileAccess, 保存为本地 .json                |
| **发布平台** | Godot Export → Steam (Steamworks SDK) / itch.io        |

## 三、 核心游戏系统

### 3.1 卦象状态机

核心逻辑层是一个由 64 条记录组成的 JSON 驱动卦象数据库，每条记录包含以下字段：

| 字段                 | 描述                                     |
| :------------------- | :--------------------------------------- |
| **id**               | 卦号 (1~64)                              |
| **name_zh**          | 卦名 (如 "乾")                           |
| **symbol**           | 卦象符号 (如 ☰)                          |
| **nature**           | 卦辞、属性及阴阳倾向                     |
| **yao_rules**        | 变爻规则 (哪一爻发动 → 转变为哪一卦)     |
| **yao_lines**        | 包含六爻原文、象传解析及白话文的详细列表 |
| **philosophy_hints** | 哲学引导候选描述列表 (供 AI 参考或重写)  |

> **设计原则：** 卦象数据库是只读的“世界规则书”。所有 AI 输出必须基于此数据库的规则而非凭空生成，确保游戏逻辑可预测且可测试。

### 3.2 交互层：结构化选项 + AI 叙事

这是最重要的差异点。为了避免自由文本输入导致 LLM 语义映射不稳定的问题，流程如下：

1.  玩家看到当前卦象及本回合局势描述 (由 AI 在上一回合生成)。
2.  玩家从四个战略范畴中选择：**“攻击、防御、计谋、和谈”**。
3.  在所选范畴下，显示 4~5 个具体行动选项 (如：“计谋” → 离间计 / 劝降 / 间谍渗透 / 盟约谈判)。
4.  选择后，游戏向 Claude API 发送结构化请求。
5.  Claude 返回 JSON 结果：**叙事文本 + 动爻判定 + 数值变化**。
6.  Godot 解析 JSON，更新卦象，播放变卦动画，更新 HUD 数值并显示叙事内容。

> **减负关键：** Claude 仅负责两件事：① 编写精彩的战争叙事段落；② 根据选项语义判定哪一爻发生变动。游戏逻辑仲裁完全由 Godot 代码处理，将 AI 的不确定性隔离在叙事层。

### 3.3 数值系统

简化为三个核心数值 (0~100)。玩家目标是在回合限制内保持数值高于临界线：

| 数值名称            | 含义                 | 临界线             |
| :------------------ | :------------------- | :----------------- |
| **国力 (Strength)** | 综合国力与战略态势   | 低于 20 → 灭国结局 |
| **民心 (Morale)**   | 军队战斗意志与凝聚力 | 低于 15 → 哗变结局 |
| **资财 (Treasury)** | 粮草与军费储备       | 低于 10 → 饥荒结局 |

每次行动后，Claude 在 JSON 中返回这三个值的增量 (delta)。Godot 执行加减并触发 UI 动画。数值间存在联动：民心低于 30 时资财消耗翻倍；资财低于 30 时国力每回合自动下降 2 点。

### 3.4 变卦演示系统

每回合结束时，Godot 根据 Claude 返回的 `yao_changed` (1~6) 查询数据库中的 `yao_rules` 来计算新卦象。动画流程：

1.  当前卦象符号逐爻高亮显示 (~0.8s)。
2.  发动的变爻闪烁红色并播放“动爻”音效。
3.  新卦象符号由下至上翻转进入视野 (~1.2s)。
4.  背景插图淡入切换为新卦象的意向图 (~0.6s)。
5.  背景音乐平滑过渡到新卦象的曲目 (~2s)。

## 四、 视觉与美术方案

### 4.1 美术风格：水墨剪影 + 像素 HUD

整体采用“水墨剪影”风格：背景使用大写意水墨渲染，角色与单位则使用像素化剪影。这创造出一种既具古典意蕴又不失现代独立游戏感的层次感。

> **参考方向：** 背景氛围类似《灵山》或《雨城》；单位像素美术类似早期版本的《最后的谋士》；UI 字体使用思源宋体 (开源)。

### 4.2 资产列表

MVP 版本的完整资产清单，按优先级分为三个级别：

| 资产类型       | 数量 / 规格                | 优先级与说明                       |
| :------------- | :------------------------- | :--------------------------------- |
| **卦象插图**   | 64 张 (512x512px 水墨)     | P0 -- 核心，通过 AI 生成并人工筛选 |
| **NPC 立绘**   | 8~12 张 (带表情的关键 NPC) | P0 -- 对话界面所需                 |
| **场景背景**   | 5~8 张 (不同战场场景)      | P0 -- 主界面背景                   |
| **UI 框架**    | 1 套 (HUD, 对话框, 菜单)   | P0 -- 像素风格，可自制             |
| **卦符动画**   | 64 套 (~6 帧 GIF)          | P1 -- 用于变卦展示                 |
| **单位像素图** | 20~30 种 (士兵, 将领)      | P1 -- 用于战场态势展示             |
| **粒子特效**   | 10~15 套 (火, 烟, 光效)    | P2 -- 后期打磨项                   |

## 五、 AI 集成技术实现

### 5.1 Claude API 调用架构

在 Godot 中创建一个单例节点 `AIManager` 管理所有 API 调用：

1.  玩家在 `HexagramConsult` 界面完成选择，触发 `AIManager.consult(payload)`。
2.  `AIManager` 构建请求 JSON 并通过 `HTTPRequest` 发送。
3.  接收到响应后，解析 `content[0].text` 提取 JSON 内容。
4.  发出信号 (Signals) 将结果广播给 `HexagramConsult` 和 `BattleResult`。
5.  场景监听信号并驱动文本展示、动画播放及数值更新。

> **错误处理：** 若 API 超时 (>8s) 或返回格式异常，`AIManager` 触发 `fallback_response()`，从本地兜底库中选取当前卦象的预设叙事，确保游戏流程不中断。

### 5.2 系统提示词 (System Prompt) 设计

控制 Claude 输出质量的核心，由四部分组成：

| 组成部分     | 内容说明                                                                                                                  |
| :----------- | :------------------------------------------------------------------------------------------------------------------------ |
| **角色设定** | “你是一位精通易经、老谋深算的古代军师，负责解读变数并提供分析。风格：简练、儒雅、深邃。”                                  |
| **任务说明** | “根据当前卦象、玩家行动及局势数值，生成战争叙事并判定动爻。”                                                              |
| **输出格式** | “必须严格返回 JSON，包含: narrative (剧情), yao_changed (1-6), delta_stats (数值变动), philosophy (判词)。严禁额外文字。” |
| **约束条件** | “叙事必须契合卦象属性。数值变动必须基于玩家决策与当前态势的逻辑合理性。”                                                  |

## 六、 游戏循环设计

### 6.1 单局流程

1.  **开场叙事：** 交代当前国情局势，起出初始卦象。
2.  **主回合循环** (单局约 15-25 回合):
    *   查看当前卦象与局势总结。
    *   在地图上观察敌我动态。
    *   选择战略范畴 (攻/防/谋/和)。
    *   选取具体行动选项 (4-5 选 1)。
    *   等待 AI 结果响应 (~2-4s)。
    *   观看动画，阅读剧情叙事。
    *   结算数值变更，进入次轮。
3.  **结局判定：** 数值触及临界线 → 触发特定坏结局；或达到回合上限 → 根据总分评级。

### 6.2 结局设计 (MVP 版)

| 结局名称     | 触发条件                                     |
| :----------- | :------------------------------------------- |
| **天下一统** | 第 25 回合所有数值 > 60 → 完美结局，世界大同 |
| **偏安一隅** | 国力 40-59，其他及格 → 普通结局，局势稳定    |
| **功败垂成** | 最后 5 回合国力急剧下降 → 遗憾结局           |
| **兵乱哗变** | 民心 < 15 → 军队哗变，被迫退位               |
| **民生凋敝** | 资财 < 10 → 大饥荒，失去支持                 |
| **国脉断绝** | 国力 < 20 → 悲惨的灭国结局                   |

## 七、 AI Agent 策略与逻辑

本章详细说明 AI Agent (Claude) 在游戏循环中的核心设计哲学与集成点。

### 7.1 核心理念：“易经状态机”
游戏将 64 卦想象为 64 种不同的**世界状态**。玩家不直接进行传统的数值堆砌，而是通过解读卦象的哲学含义来进行战略干预。

- **世界状态 (卦象)**：定义了 NPC 的行为倾向和底层的战术氛围。
- **三才数值 (三才数值)**：
    - **实力 (实力/天)**：军事力量与领土。
    - **民心 (民心/人)**：百姓支持与军魂。
    - **资财 (资财/地)**：经济物流与后勤资源。

### 7.2 Agent 实施要点
AI Agent (Claude API) 通过以下机制兼任**叙事者**与**逻辑仲裁者**：

#### **A. 语义映射 (逻辑层)**
当玩家选择动作（如“离间计”）时：
- **Agent 解读**：Agent 根据当前卦象分析动作的“阴阳”倾向。
- **动爻识别**：识别出卦象中受影响最大的那一爻 (1-6)。
- **状态转换**：Godot 使用此索引切换到“变卦”，从根本上改变战术面貌。

#### **B. 动态战争叙事 (叙事层)**
Agent 将抽象的数值变动转化为具有高度历史/哲学感的文本。
- **输入**：当前卦象 + 玩家动作 + 当前数值。
- **输出**：100-200 字的回报或判词，保持一致的历史风格。

#### **C. 自适应平衡 (数值层)**
Agent 根据具体的“情境”评估决策的明智程度，从而决定数值增减，而非固定数值。
- **示例**：在“困”卦下进行激进扩张，相比在“乾”卦下，会导致更重的资财与民心扣罚。

## 八、 模块化开发路线图 (AI 辅助参考)

通过将开发计划转化为“功能模块”，确保高内聚低耦合。

### 模块 1：核心数据与状态机
*   **1.1 卦象数据库**：解析 `hexagrams.json`，创建 `DatabaseManager` 单例缓存全量数据。
*   **1.2 规则引擎**：实现由“当前卦 + 变爻”推导出“下个卦”的逻辑算法。
*   **1.3 游戏状态管理器**：维护国力、民心、资财数值，通过信号 (`stats_changed`) 驱动 UI 更新。

### 模块 2：AI 交互与后端集成
*   **2.1 HTTP 交互管理器**：封装 Godot `HTTPRequest` 生命周期，实现异步信号机制与超时重试。
*   **2.2 提示词构建器 (Prompt Builder)**：将局势上下文、数值与玩家动作拼装为 System Prompt。
*   **2.3 响应解析器**：通过正则提取 Claude 返回的 JSON 字符串并进行错误校验。

### 模块 3：UI 组件与场景编排
*   **3.1 交互面板 (HexagramConsult)**：构建层级化 UI，支持数据驱动的选项生成。
*   **3.2 变色演出协调器**：实现起卦、动爻、翻转、新卦展示的视觉流程。
*   **3.3 叙事系统**：处理 RichTextLabel 的打字机效果及哲学判词格式化展示。

## 九、 附录

### A. 64 卦样本数据结构 (JSON)
```json
{
  "id": 1,
  "name_zh": "乾",
  "symbol": "☰",
  "nature": "纯阳, 天, 创造力",
  "yao_rules": {
    "1": 44, "2": 33, "3": 10, "4": 9, "5": 14, "6": 43
  },
  "yao_lines": [ ... ]
}
```

## 十、 开发进展 (2026-04-10)

### 10.1 底层架构重构
*   **全动态 UI 生成 (Programmatic UI)**：重构了所有核心 UI 组件（StatsHUD, ActionPanel, NarrativeBox, HexagramDisplay）。现在组件在挂载时动态生成节点结构，消除了 Godot `.tscn` 文件可能导致的场景 UID 冲突风险，极大提升了系统稳定性和代码的可维护性。
*   **Tween 动画系统**：实装了战术数值条的平滑缓动 (`Tween`) 效果、卦象切换时的爆闪特效以及自适应长度的打字机叙事系统。

### 10.2 易经数据库完备化
*   **全量数据导入**：通过 Python 解析脚本对 64 卦典籍进行了自动化处理。
*   **内容深度扩充**：`hexagrams.json` 已从 16KB 填充至 280KB 以上，每卦均包含：
    *   **典籍原文**：保留《易经》原本文字与《象传》解析。
    *   **爻位详情**：六爻原文、白话翻译、邵雍吉凶星级。
    *   **战略映射**：提取了时运、财运作为游戏“三才数值”变化的底层参考依据。

### 10.3 系统功能优化
*   **配置持久化**：实现了 Claude API Key 的本地持久化存储 (`user://settings.cfg`)。玩家输入的密钥将跨 Session 保留，显著优化了用户设置流程。
*   **逻辑闭环绑定**：打通了 `TurnController` 与 `AIManager` 之间的信号链路，初步形成了“决策-API请求-动画展示-数值结算”的交互闭环。
