# 易经大战略：执道者 (I Ching Grand Strategy: The Executor)

| 属性         | 说明                       |
| :----------- | :------------------------- |
| **引擎**     | **Godot 4**                |
| **视角**     | **2D 策略 / 视觉小说混合** |
| **AI 后端**  | **Claude API**             |
| **团队规模** | **2人（程序 + 美术）**     |
| **开发周期** | **MVP 约 4~5 个月**        |

## 一、项目概述

本方案在原「易经大战略：执道者」概念基础上，针对引擎选型、交互方式与视觉实现进行降难修订，在完整保留策略游戏核心体验的前提下，将开发难度从 ★★★★☆ 降至 ★★★☆☆，使2人小团队能够在4~5个月内完成可玩 MVP。

*   **核心保留：** 玩家扮演军师，通过解读卦象干预战局；64卦状态机驱动 NPC 行为；多结局数值目标系统。
*   **核心修改：** 引擎由 Unity/Unreal 改为 Godot 4；3D画面改为 2D水墨像素风；自由文本输入改为结构化选项 + AI 叙事。

### 1.1 一句话定位

《执道者》是一款 2D 水墨风策略 + 视觉小说游戏：玩家以军师身份，通过解读周易卦象、选定策略方向，引导 AI 生成战争叙事，最终影响国家兴衰的结局。

### 1.2 目标受众

*   对中国传统文化、历史策略感兴趣的玩家（18~40岁）
*   喜欢视觉小说、有叙事深度的轻策略游戏玩家
*   Steam / itch.io 独立游戏受众

## 二、引擎与技术栈



### 2.2 完整技术栈

| 模块            | 方案                                                    |
| :-------------- | :------------------------------------------------------ |
| **游戏引擎**    | Godot 4.x（GDScript 为主，C# 可选）                     |
| **AI 后端**     | Claude API（claude-sonnet-4-5，结构化 JSON 输出）       |
| **美术工具**    | Aseprite（像素图）/ Procreate + Photoshop（水墨素材）   |
| **音频工具**    | FMOD 或 Godot 内置 AudioStreamPlayer + OGG 资源         |
| **版本控制**    | Git + GitHub（.godot 文件用 .gitignore 排除二进制缓存） |
| **配置管理**    | JSON 文件存储卦象数据库（约 64 条记录）                 |
| **存档系统**    | Godot 内置 FileAccess，存为本地 .json                   |
| **构建 & 发布** | Godot Export → Steam（Steamworks SDK）/ itch.io         |

## 三、核心游戏系统

### 3.1 卦象状态机（Hexagram State Machine）

游戏的核心逻辑层是一个 JSON 驱动的卦象数据库，共 64 条记录，每条记录包含以下字段：

| 字段                  | 说明                                        |
| :-------------------- | :------------------------------------------ |
| **id**                | 卦象编号（1~64）                            |
| **name_zh / name_en** | 卦名（如「乾」/ "Qian"）                    |
| **symbol**            | 卦象符号（如 ☰）                            |
| **nature**            | 五行属性与阴阳倾向                          |
| **npc_tendency**      | 此卦下 NPC 的行为倾向描述（供 Claude 参考） |
| **yao_rules**         | 六爻变卦规则表（哪爻动 → 变为哪卦）         |
| **image_key**         | 对应的预制插图资源 key                      |
| **bgm_key**           | 对应的背景音乐 key                          |
| **philosophy_hints**  | 哲学引导语候选列表（AI 可选用或改写）       |

> **设计原则：** 卦象数据库是只读的「世界规则」，AI 的所有输出必须基于此数据库中的规则，而不是自由发挥。这保证了游戏逻辑的可预测性与可测试性。

### 3.2 交互层：结构化选项 + AI 叙事

这是与最关键的差异点。让玩家自由输入文本，由 LLM 实时语义映射——这在工程上不稳定。改造后的流程如下：

1.  玩家在游戏界面看到当前卦象，以及本回合的局势描述（由 AI 上一回合生成）。
2.  玩家从「攻、守、谋、和」四大策略类别中选择一类。
3.  该类别下展示 4~5 个具体行动选项（例如「谋」→ 离间计 / 招降书 / 细作渗透 / 结盟谈判）。
4.  玩家点选行动后，游戏向 Claude API 发送结构化请求（见 §5 技术实现）。
5.  Claude 返回 JSON 格式的结果：叙事文字 + 爻动判定 + 数值变化。
6.  Godot 解析 JSON，更新卦象、播放变卦动画、更新 HUD 数值、显示叙事文字。

> **降难关键：** Claude 只负责两件事：①写好看的战争叙事段落；②依据选项语义判断哪条爻变动。游戏的规则仲裁完全由 Godot 代码执行，AI 的不稳定性被隔离在叙事层，不会影响游戏逻辑。

### 3.3 数值系统

精简为三条核心数值，各自范围 0~100，玩家目标是在回合限制内将三者维持在合格线以上：

| 指标名称     | 含义                 | 临界线             |
| :----------- | :------------------- | :----------------- |
| **天下大势** | 综合国力与战略态势   | 低于 20 → 亡国结局 |
| **军心士气** | 军队战斗意志与凝聚力 | 低于 15 → 兵变结局 |
| **国库储备** | 粮草钱粮的存量       | 低于 10 → 饥荒结局 |

每次玩家行动后，Claude 在 JSON 中返回三个数值的 delta（变化量），Godot 执行加减并触发对应的 UI 动画。数值互有联动：军心跌破 30 时，国库消耗速率加倍；国库跌破 30 时，天下大势每回合自动下降 2 点。

### 3.4 卦象变卦系统

每回合结束后，Godot 根据 Claude 返回的 `yao_changed`（爻变序号，1~6）查询卦象数据库中的 `yao_rules`，自动计算新卦象。变卦动画流程：

1.  当前卦象符号各爻依次高亮（约 0.8 秒）
2.  变爻位置闪烁红色，发出「爻动」音效
3.  新卦象符号从下至上逐爻翻转显示（约 1.2 秒）
4.  背景插图淡出至新卦象插图（约 0.6 秒）
5.  BGM 交叉淡化至新卦象对应音轨（约 2 秒）

## 四、视觉与美术方案

### 4.1 美术风格：水墨剪影 + 像素 HUD

整体采用「水墨剪影」风格——背景为大尺寸水墨渲染图，人物与单位为像素剪影风格，两者形成层次感，既有古典意境，又有现代独立游戏质感。

> **参考方向：** 背景气质参考《山海旅人》《雨纪》；单位像素风参考《明末弧臣传》早期版本；UI字体使用思源宋体（开源免费）。

### 4.2 美术资源清单

以下为 MVP 所需的完整美术资产，优先级分三级：

| 资产类型         | 数量 / 规格                     | 优先级与说明                               |
| :--------------- | :------------------------------ | :----------------------------------------- |
| **卦象插图**     | 64张（512×512px 水墨风）        | P0 -- 核心，可用 AI 生图批量生成后人工筛选 |
| **NPC 立绘**     | 8~12 张（主要 NPC，含表情变体） | P0 -- 对话界面必需                         |
| **地图背景**     | 5~8 张（不同战场场景）          | P0 -- 主界面背景                           |
| **UI 框架**      | 1套（HUD、对话框、菜单）        | P0 -- 像素风，开发者可自制                 |
| **卦象符号动画** | 64组（每组约 6 帧 GIF）         | P1 -- 变卦动画用                           |
| **单位像素图**   | 20~30 个单位（士兵、将领等）    | P1 -- 战场地图展示用                       |
| **粒子效果**     | 10~15 组（火焰、烟雾、金光）    | P2 -- 润色，可后期添加                     |

### 4.3 场景结构

游戏共分以下主要场景（Godot Scene），各场景职责单一：

| 场景名称              | 功能                                              |
| :-------------------- | :------------------------------------------------ |
| **MainMenu**          | 主菜单：开始 / 继续 / 设置                        |
| **WorldMap**          | 战略地图：显示各势力控制区，选择本回合行动地区    |
| **HexagramConsult**   | 卦象咨询主界面：卦象显示、策略选择、叙事文字、HUD |
| **BattleResult**      | 回合结算界面：数值变化动画、变卦过场              |
| **NPCDialogue**       | NPC 对话界面：立绘 + 对话气泡（视觉小说风格）     |
| **GameOver / Ending** | 结局界面：多结局剧情呈现                          |
| **Settings**          | 设置界面：音量、字体大小、API Key 输入            |

## 五、AI 集成技术实现

### 5.1 Claude API 调用架构

在 Godot 中创建一个单例节点 `AIManager`，统一管理所有 Claude API 调用。调用流程：

1.  玩家在 `HexagramConsult` 场景完成选择，触发 `AIManager.consult(payload)`
2.  `AIManager` 构建请求 JSON，通过 `HTTPRequest` 节点 POST 至 Claude API
3.  收到响应后，`AIManager` 解析 `content[0].text`，提取 JSON 结果
4.  通过信号（Signal）将结果广播至 `HexagramConsult` 和 `BattleResult` 场景
5.  各场景监听信号，分别驱动叙事文字、变卦动画、数值更新

> **容错设计：** 若 API 调用超时（超过 8 秒）或返回格式异常，`AIManager` 自动触发 `fallback_response()`，从本地预设库中随机选取一条该卦象的默认叙事，确保游戏流程不中断。

### 5.2 系统提示词设计

系统提示词是控制 Claude 输出质量的核心，需包含以下四个部分：

| 提示词部分   | 内容                                                                                                                                                                                                                                                          |
| :----------- | :------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| **角色设定** | 「你是一位精通周易的古代军师智囊，负责解读卦象变化，为主将提供战略分析。你的语言风格：简练、古雅，有哲学深度，但不晦涩难懂。」                                                                                                                                |
| **任务规范** | 「根据当前卦象、玩家行动和局势数值，生成一段战争叙事，并判断本次行动引发的爻动情况。」                                                                                                                                                                        |
| **输出格式** | 「必须严格返回 JSON 格式，包含以下字段：narrative（叙事段落，200字以内）、yao_changed（变爻序号1-6，无变化则为0）、delta_势（数值变化，-15至+15）、delta_士（同上）、delta_储（同上）、philosophy（哲学引导语，30字以内）。除 JSON 外不得输出任何其他内容。」 |
| **约束条件** | 「所有叙事必须符合当前卦象的五行属性与阴阳倾向（见 context 中的卦象描述）。数值变化需与玩家行动类型和当前局势数值相符，避免出现脱离逻辑的极端数值。」                                                                                                         |

### 5.3 请求与响应示例

以下为一次完整的 API 交互示例（乾卦 → 玩家选择「谋 · 离间计」）：

**用户消息（user message）示例：**

```markdown
当前卦象：乾（☰）——阳刚雄健，天行健，君子以自强不息
当前数值：天下大势 62 / 军心士气 55 / 国库储备 48
本回合行动：谋 · 离间计（向敌方将领散布谣言，动摇其君主信任）
当前局势：北方来犯，敌将韩陵手握十万精兵，我方需在三回合内化解威胁。
```

**Claude 返回 JSON 示例：**

```json
{
  "narrative": "细作潜入韩营，携密信一封，言称韩陵私通西戎。消息三日内传至朝堂，君主召韩陵回京问询。北疆战云骤散，然军中人心浮动，将士不知虚实，观望者众。",
  "yao_changed": 3,
  "delta_势": 8,
  "delta_士": -5,
  "delta_储": -3,
  "philosophy": "九三：君子终日乾乾，夕惕若厉，无咎。"
}
```

Godot 解析此 JSON：将 `yao_changed=3` 传入卦象状态机，乾卦第三爻动，变为「夬卦（☴）」；delta 值分别应用到三条数值；`narrative` 显示在对话框；`philosophy` 显示在卦象框下方。

## 六、游戏流程设计

### 6.1 单局流程

1.  **开局叙事：** 介绍当前国家局势，随机起卦，生成初始卦象。
2.  **主回合循环**（约 15~25 回合/局）：
    *   查看当前卦象与局势描述
    *   查看世界地图，了解各势力动态
    *   选择策略大类（攻/守/谋/和）
    *   选择具体行动（4~5 选 1）
    *   等待 AI 返回结果（约 2~4 秒）
    *   观看变卦动画，阅读叙事文字
    *   查看数值变化，进入下一回合
3.  **终局判定：** 数值触发临界线 → 特定结局；或回合数到达上限 → 按综合分计算结局等级。
4.  **结局呈现：** 水墨动画 + 语音旁白 + 历史典故延伸阅读。

### 6.2 结局设计（MVP 版）

MVP 阶段设计 6 种结局，覆盖主要数值路径：

| 结局名称     | 触发条件与描述                                        |
| :----------- | :---------------------------------------------------- |
| **天下归一** | 三项数值均在 60 以上完成 25 回合 → 最佳结局，天下一统 |
| **偏安江南** | 天下大势 40~59，其余合格 → 良好结局，保守固守一方     |
| **功败垂成** | 最后五回合天下大势骤降 → 遗憾结局，功亏一篑           |
| **兵变末路** | 军心士气跌破 15 → 军队哗变，被迫退位                  |
| **赤地千里** | 国库储备跌破 10 → 饥荒蔓延，民心尽失                  |
| **覆国之殇** | 天下大势跌破 20 → 亡国结局，最悲剧走向                |

## 七、模块化开发路线指导（供 AI 辅助开发参考）

为了更好地引导 AI 进行项目拆解与代码生成，我们将原有的时间线开发计划转换为以系统功能为单位的「模块化开发路线」。AI 在执行具体任务时，应严格遵循该划分，确保内部架构高内聚、低耦合。

### 模块一：核心数据与状态机系统 (Data & State Machine)

本模块旨在确立游戏的底层规则驱动机制，是后续所有交互和渲染的基石。

*   **1.1 卦象数据库 (Hexagram Database)**
    *   **任务**：构建并解析 `hexagrams.json`，确保包含 64 卦的 ID、五行属性、爻变规则矩阵和提示词语料库。
    *   **指导**：在 Godot 中建立一个单例 `DatabaseManager`，在游戏初始化时自动加载并缓存该 JSON 为持久化字典。
*   **1.2 变卦规则引擎 (Rule Engine)**
    *   **任务**：实现从“当前卦+动爻”推演“下卦”的核心算法。
    *   **指导**：提供一个纯逻辑方法 `get_next_hexagram(current_id, yao_changed_index)` 供外部模块调用。该方法应符合纯函数特性，无任何副作用。
*   **1.3 核心状态与数值管理 (GameState Manager)**
    *   **任务**：防篡改地维护“大势”、“军心”、“国库”三项重要数值，及当前回合数。
    *   **指导**：使用独立单例管理器 `GameState`，只能通过特定 API（例如 `apply_delta(delta_dict)`）更新数值，并在更新后向全系统广播 `stats_changed` 信号。

### 模块二：AI 交互与后端通信系统 (AI Backend Integration)

负责 Godot 与大语言模型（Claude API）之间的请求构建、异步通讯及容错降级。

*   **2.1 HTTP 异步通信内核 (AIManager)**
    *   **任务**：封装 Godot 内置的 `HTTPRequest` 节点，完成从发送 POST 请求到接收响应流的完整生命周期。
    *   **指导**：全面使用异步信号机制。必须要实现“超时检测”（Timeout）机制，若接口返回 5xx 等网络异常，应立刻平滑降级至本地预置文字库 (Fallback)，绝不能让接口卡住游戏主循环。
*   **2.2 提示词工程聚合器 (Prompt Builder)**
    *   **任务**：动态组装请求，将当前世界观、数值上下文档、卦象特性、玩家动作合并为符合 System Prompt 结构的话术。
    *   **指导**：严禁将 UI 层的松散文本直接透传给 AI。应当在代码中维护标准映射关系，确保推给 Claude 的格式具有高度的前后一致性。
*   **2.3 JSON 响应解析网关 (Response Parser)**
    *   **任务**：剥离并清洗 Claude 返回的数据体，抽取游戏真正需要的参数字段。
    *   **指导**：需防备模型产生的“带 markdown 标识 (` ```json `)” 的多余文本。利用正则匹配边界提取纯净的 JSON 字符串送给 `JSON.parse`。

### 模块三：UI 组件与场景调度逻辑 (UI & Scenes)

负责视觉呈现、数据绑定与玩家输入的监听。

*   **3.1 测卦互动面板 (HexagramConsult 面板)**
    *   **任务**：搭建分层多态的 UI 系统（包含卦名显示、对应插图、行动分类按钮组、状态属性条、动态叙事版）。
    *   **指导**：按钮矩阵应做到数据驱动生成；当请求发送给后端时，在此模块级临时接管输入层（如全屏置灰或激活 Loading 组件）。
*   **3.2 变卦视觉演出器 (Animation Orchestrator)**
    *   **任务**：实现起卦、定爻、爻变、展示新卦四个视觉阶段及其附属粒子/音效反馈。
    *   **指导**：彻底弃用硬编码计时器，全面转向 `Tween` 节点组合式序列动画（Sequence）和 `AnimationPlayer` 操作，以便维护动画时序链。
*   **3.3 叙事打字机与表现器 (Dialogue System)**
    *   **任务**：确保后端传回的文本在 UI 中的拟人化展现。
    *   **指导**：提供平滑滚字特效。支持富文本标签 (`RichTextLabel`)，并实现自动在“哲学引导语”前后注入高亮排版结构。

### 模块四：主生命周期与结算闭环 (Game Loop Manager)

负责统筹局内节奏循环、结局拦截的骨干模块。

*   **4.1 回合推进器 (Turn Logic)**
    *   **任务**：把控“通报局势 -> 选取战法 -> API 回调 -> 呈现判定 -> 更新指标”这样一套完整的时序轮回。
    *   **指导**：每经历一阶段需触发严格的状态锁，并在切换阶段时对外释放对应信号机制。
*   **4.2 结局路由 (Ending Router)**
    *   **任务**：每回合状态落定后，严格审查任何核心数值是否越界（低于阈值触发特型结局，或达到最大回合计算综合评测）。
    *   **指导**：结局条件一旦命中，利用路由模块强制截断主干流程，释放局内内存，平滑载入结局专属水墨短片（预制件）与旁白模块。



## 十、附录

### A. 64 卦示例数据结构（JSON 片段）

```json
{
  "id": 1,
  "name_zh": "乾",
  "name_en": "Qian",
  "symbol": "☰",
  "nature": "纯阳，天，刚健",
  "npc_tendency": "NPC 倾向主动进攻，扩张领土，不畏风险。",
  "yao_rules": {
    "1": 44, "2": 33, "3": 10, "4": 9, "5": 14, "6": 43
  },
  "image_key": "hexagram_01_qian",
  "bgm_key": "bgm_qian_majestic",
  "philosophy_hints": [
    "天行健，君子以自强不息。",
    "潜龙勿用，阳气潜藏。",
    "飞龙在天，利见大人。"
  ]
}
```